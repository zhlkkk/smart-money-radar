[build]
dockerfilePath = "Dockerfile"
watchPatterns = ["apps/backend/**", "packages/shared/**", "packages/db/**", "pnpm-lock.yaml"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
